= Release Guidelines

Release from the .x release branch, e.g. 1.0.x usign the Build and Deploy workflow.

Merge-forward using -s ours to main.

Move the v1 tag forward to match the new tag for the given release you are performing.
