plugins {
	id "io.spring.convention.root"
}

group = "io.spring.release"
description = "Spring Release Tools"

if (hasProperty("buildScan")) {
	buildScan {
		termsOfServiceUrl = "https://gradle.com/terms-of-service"
		termsOfServiceAgree = "yes"
	}
}
